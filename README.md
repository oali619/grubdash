# grubdash


## Backend

Open the `server` directory: 

```
cd server
```

Install dependencies:

```
npm install
```

Run backend:

```
npm start
```
## Client

Open the `client` directory:

```
cd client
```

Install dependencies:

```
npm install
```

Run frotnend:

```
npm start
```