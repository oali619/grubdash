# grubdash


## Backend

Open the `server` directory: 

```
cd server
```

Install dependacies:

```
npm install
```

Run backend:

```
npm start
```
## Client

Open the `client` directory:

```
cd client
```

Install dependacies:

```
npm install
```

Run frotnend:

```
npm start
```